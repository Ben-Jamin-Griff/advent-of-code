use std::fs;


fn main() {

    let _input = fs::read_to_string("./input.txt").expect("Could not parse input.txt!");

    let mut safe_codes: Vec<i32> = new::Vec;

    for line in _input.lines() {
       let parts: Vec<&str> = line.split_whitespace().collect();

       let mut first_val: i32 = part[0];

       for part in &parts[1:] {
    }

    println!("Finito");
}
